# NOTE: Use Python 3.11 (or 3.10). Some dependencies do not yet support Python 3.13 — using 3.11 avoids metadata/build errors.
# Core API
openai>=1.0.0,<2.0.0

# Env / config
python-dotenv>=1.0.0,<2.0.0

# Validation / models
pydantic>=1.10.0,<3.0.0

# Audio / speech
SpeechRecognition>=3.8.1
PyAudio>=0.2.14,<0.3.0; sys_platform == "win32"

# Realtime client (install from Git repository)
openai-realtime-client @ git+https://github.com/run-llama/openai_realtime_client.git@main#egg=openai_realtime_client

# Indexing and input
llama-index>=0.10.0
pynput>=1.7.6
